# Port of Terminal.app's "Basic" profile
theme = Terminal Basic
font-family = SF Mono
font-size = 11

# Terminal.app thickens strokes via macOS smoothing; Ghostty doesn't by default
font-thicken = true

cursor-color = #000000
